# Shadows src/main/resources/application.properties in test scope, so it must be self-contained.
# Tests must not require a running SOFARegistry: use sofa-rpc's file-based local registry.
spring.application.name=payment-gateway
com.alipay.sofa.rpc.registry.address=local:///tmp/paylab-test-local-registry
com.alipay.sofa.rpc.bolt.port=13200
server.port=0
